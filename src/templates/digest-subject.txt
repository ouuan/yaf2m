{% set feed_title = feeds[0].title.content if feeds[0].title else "Feed" %}[{{ now() | dateformat(format="short", tz=template_args.tz or "UTC") }}] {{ items | length }} new item{{ "s" if items | length > 1 else "" }} from {{ feed_title }}{% if feeds | length > 1 %} (+{{ feeds | length - 1 }}){% endif %}
